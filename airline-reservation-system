✈️ Airline Reservation System – Java Spring Boot + JSP + MySQL

A simple and fully working Airline Reservation System built using:

✔ Spring Boot
✔ JSP (Java Server Pages)
✔ Spring MVC
✔ Spring Data JPA
✔ MySQL
✔ HTML/CSS/JSP Forms

This project allows users to search flights, book tickets, select food, view booking history, and cancel bookings. Admins can add new flights.

🚀 Features
👤 User Features

Search available flights

Book flight tickets

Choose food items

Automatic price calculation

View booking history

Cancel booking (restores seats)

🛠 Admin Features

Admin login

Add new flights

View all bookings

🏗️ Tech Stack
Technology	Purpose
Java 21	Backend programming
Spring Boot 3/4	Framework
Spring MVC	Controllers + Views
JPA / Hibernate	Database ORM
MySQL	Database
JSP + JSTL	Frontend UI
CSS	Styling
📦 Project Structure
src/main/java
 ├── controller
 ├── model
 ├── repository
 └── AirlineReservationSystemApplication.java

src/main/webapp
 └── WEB-INF
      └── views (JSP files)

🔧 Setup Instructions
1️⃣ Clone repository
git clone https://github.com/YOUR_USERNAME/airline-reservation-system.git

2️⃣ Create MySQL Database

Run this query:

CREATE DATABASE airdb;

3️⃣ Update application.properties
spring.datasource.url=jdbc:mysql://localhost:3306/airdb
spring.datasource.username=root
spring.datasource.password=YOUR_PASSWORD

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

4️⃣ Run the application

Using Eclipse/IntelliJ:

Right click → Run as → Spring Boot App


Or using terminal:

mvn spring-boot:run
